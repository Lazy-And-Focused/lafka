services:
  app:
    build:
      context: .
      dockerfile: dockerfile

    ports:
      - "8080:8080"
    environment:
      - PROGRAM_MODE=production
    volumes:
      - ./:/app
      - /app/node_modules
    working_dir: /app
    command: node ./dist/main.js
